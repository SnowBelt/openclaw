// Compatibility boundary for custom Control UI imports. Keep the redaction
// implementation canonical so upstream security additions apply here too.
export { redactToolDetail, redactToolPayloadText } from "../lib/browser-redact.js";
